[pytest]
testpaths = tests
# tests/ holds conftest.py's helpers (make_user, login, ...) and importing them
# by name needs tests/ itself on sys.path.
pythonpath = . tests
addopts = -q --strict-markers
filterwarnings =
    ignore::DeprecationWarning:flask.*
